# S6-07f: Eval Migration, Legacy Removal & Tests

> **Parent:** B6-07 (Eval Hub — Polymorphic Targets & Flow Tracing)  
> **Status:** `[x]`  
> **Complexity:** 🟡 Medium  
> **Owner:** `migrations`, `projects/evalops`, `tests`  

---

## Scope

Complete the Eval Hub cutover. Backfill every existing suite, run, metric result and flow trace into the
`eval/default` seed hub with `target_type="agent"` and `target_hub_id` pointing at `agent/default`,
tighten the new `hub_id` and target columns to `NOT NULL`, and drop the V5 `agent_id` columns. Then
delete the flat `/api/evalops/*` suite and run routes from
[`projects/evalops/api.py`](file:///c:/Akshat/ContAIned/projects/evalops/api.py) and land
`tests/test_eval_hub.py`, the acceptance suite for B6-07.

## Actionables

- [x] Add Alembic revision `migrations/versions/<rev>_eval_hub_backfill.py` (depends on the S6-07a revision and the S6-01e seed revision) that:
  - Resolves the `eval/default` and `agent/default` hub ids, aborting with a clear `RuntimeError` if either is absent
  - `UPDATE eval_test_suites SET hub_id = :eval_hub, target_type = 'agent', target_hub_id = :agent_hub, target_id = agent_id WHERE hub_id IS NULL`
  - `UPDATE eval_run_history SET hub_id = :eval_hub, target_type = 'agent', target_hub_id = :agent_hub, target_id = agent_id WHERE hub_id IS NULL`
  - `UPDATE eval_flow_traces SET hub_id = :eval_hub WHERE hub_id IS NULL` — except rows already assigned a workflow hub by the S6-06a backfill, which are left untouched
  - De-duplicates suite names within the seed hub before creating `uq_eval_suites_hub_name`, suffixing ` (2)`, ` (3)`… on collision
  - `ALTER COLUMN ... SET NOT NULL` on `hub_id`, `target_type`, `target_hub_id` and `target_id` for suites and runs, and on `eval_flow_traces.hub_id`
  - `op.drop_column("eval_test_suites", "agent_id")` and `op.drop_column("eval_run_history", "agent_id")` **last**, only after the target triple is verified populated
- [x] Seed the required grants if S6-01e did not: `eval/default → agent/default` and `eval/default → workflow/default` rows in `hub_links` with `access_level='use'`, each guarded by an existence check. Assert that no link is created *into* an eval hub, per [`hubs.md`](file:///c:/Akshat/ContAIned/agent_buildable_base/references/logic/hubs.md) §3.3.
- [x] Make the revision idempotent (`WHERE hub_id IS NULL` guards, `NOT EXISTS` on link inserts) and implement `downgrade()`: re-add `agent_id`, populate it from `target_id` where `target_type='agent'`, drop the polymorphic columns, and restore the V5 unique constraint on suite names.
- [x] Delete the flat routes from [`projects/evalops/api.py`](file:///c:/Akshat/ContAIned/projects/evalops/api.py): every suite handler (`/suites`, `/suites/{suite_id}`, `/suites/{suite_id}/clone`, `/suites/{suite_id}/cases`, `/cases/{case_id}`, `/suites/{suite_id}/import`, `/suites/{suite_id}/export`), the run handlers (`/run`, `/runs/{agent_id}`, `/runs/detail/{run_id}/metrics`, `/runs/{run_id}/traces`, `/test-cases/{agent_id}`), `/generate`, and the flat `/dashboard*` and `/reports/*` handlers superseded by S6-07e.
- [x] Keep only genuinely platform-level endpoints on that router — `GET /status` — and remove the router's mount point from [`gateway/main.py`](file:///c:/Akshat/ContAIned/gateway/main.py) / [`gateway/api/proxy.py`](file:///c:/Akshat/ContAIned/gateway/api/proxy.py) if it no longer serves hub-scoped paths.
- [x] Grep for and update every remaining caller of the removed surface: `"/api/evalops/"` and `"/evalops/"` literals in `frontend/src/services`, `projects/guardroute/src/nodes/eval_executor.py`, [`scripts/deploy.py`](file:///c:/Akshat/ContAIned/scripts/deploy.py) and `tests/`. In particular `eval_executor.py` (the workflow `EvalNode`) must call the hub-scoped run path with the workflow hub's linked eval hub, not the flat one.
- [x] Port the still-valid assertions from [`tests/test_eval_schemas.py`](file:///c:/Akshat/ContAIned/tests/test_eval_schemas.py) and [`tests/test_v2_evalops_pipeline.py`](file:///c:/Akshat/ContAIned/tests/test_v2_evalops_pipeline.py) and delete those that assume a single global agent-bound suite namespace.
- [x] Create `tests/test_eval_hub.py` with fixtures for two eval hubs (`eval/alpha`, `eval/beta`), one agent hub and one workflow hub linked only to `alpha`, and users at each hub role. Cover:
  - **Polymorphic targeting** — a suite targeting an agent and a suite targeting a workflow both run and produce `eval_run_history` rows with the correct target triple
  - **Link enforcement** — creating a suite in `beta` against `alpha`'s linked agent returns `403 HUB_LINK_REQUIRED`; revoking `alpha`'s link then running returns `403 HUB_LINK_REVOKED`
  - **Cross-hub mismatch** — a `target_id` from the workflow hub paired with the agent hub's `target_hub_id` returns `422 CROSS_HUB_REFERENCE_MISMATCH`
  - **Workflow trace assertions** — a workflow-targeted run persists `eval_flow_traces` linked by `workflow_run_id`, and a node assertion on an executed node produces an `eval_metric_results` row with `framework="node_assertion"`; an assertion on a node that did not execute fails rather than passes
  - **Hub isolation** — every route in the S6-07e table returns `404` for a suite, case, run or trace owned by `beta`
  - **Dataset safety** — a CSV import carrying a foreign `suite_id` column is rejected wholesale with no partial writes
  - **Migration behaviour** — a fixture V5 suite backfills to `target_type="agent"` with `target_hub_id` = `agent/default`
- [x] Extend `tests/test_hub_isolation.py`'s static guard to `EvalTestSuite`, `EvalTestCase`, `EvalRunHistory`, `EvalMetricResult` and `EvalFlowTrace`, failing the build on any primary-key-only query under `gateway/` or `projects/`.
- [x] Verify end-to-end against a populated V5 dump: `alembic upgrade head`, assert every suite and run carries a non-null `hub_id` and target triple and that no `agent_id` column remains on either table, assert suite/run/metric/trace row counts are unchanged, then `downgrade -1` / `upgrade head` and re-assert.

## Dependencies
- S6-07a, S6-07b, S6-07c, S6-07d, S6-07e
- S6-01e (seed hubs & links), S6-05e (agent backfill), S6-06g (workflow backfill — trace hub assignment must run first)

## References
- [`references/logic/evalops.md`](file:///c:/Akshat/ContAIned/agent_buildable_base/references/logic/evalops.md)
- [`references/logic/hubs.md`](file:///c:/Akshat/ContAIned/agent_buildable_base/references/logic/hubs.md) §3.3, §6
- [`references/logic/workflow_v6.md`](file:///c:/Akshat/ContAIned/agent_buildable_base/references/logic/workflow_v6.md) §2.3, §6
- [`tasks/v6/base/B6-11_legacy_decommission_verification.md`](file:///c:/Akshat/ContAIned/agent_buildable_base/tasks/v6/base/B6-11_legacy_decommission_verification.md)
