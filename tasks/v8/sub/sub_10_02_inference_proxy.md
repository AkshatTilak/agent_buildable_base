# Subtask 10_02: Inference Proxy Live Tests

## Parent Base Task
`tasks/v8/base/10_live_api_tests.md`

## Objective
Create `tests/live_api/test_inference_proxy.py` — OCR & embedding endpoints.

## Tasks
1. [ ] Proxy request through gateway to inference server (if running).
2. [ ] Verify OCR endpoint with a real image.
3. [ ] Verify embedding endpoint with real text.

## Definition of Done
- Inference proxy OCR/embedding endpoints verified.
- Proxy request through gateway to inference server verified.
