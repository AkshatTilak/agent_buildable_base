# Subtask: Docker-Compose Volumes

**Parent Link:** `base/system_lifecycle.md`

## Actionable Steps
- [x] Define global external/internal volume mappings in docker-compose.
- [x] Declare pgdata, qdrant_data, neo4j_data, neo4j_logs, redis_data, and kafka_data volumes.

## Dependencies
- Docker storage engine.

## Definition of Done
- Volume declarations are complete and allow data persistence across container updates/restarts.
