# S5-01h: Frontend — Results Dashboard (Metrics Cards, Trend Charts, Drill-Down)

> **Parent:** B5-01 (EvalOps Deep Integration)  
> **Status:** `[x]`  
> **Complexity:** 🟡 Medium  
> **Owner:** `frontend`

---

## Scope

Replace mock-data dashboard with real metrics dashboard powered by stats/trends APIs and per-run metric drill-downs.

## Actionables

- [x] Create `frontend/src/components/eval/MetricsCards.tsx` & embed in `EvalPanel.tsx`:
  - Faithfulness, Relevance, Recall, Precision, Context Recall, Answer Relevance, Hallucination Rate, Toxicity Score, Bias Score
- [x] Create trend line chart visualization in `EvalPanel.tsx`:
  - Selected metrics over time with 7d, 30d, 90d date range selector and per-agent filtering
- [x] Create Run History & Drill-Down in `EvalPanel.tsx`:
  - Table of evaluation history runs with expandable rows showing granular per-case per-metric scores, thresholds, framework badges, and reasons
- [x] Create `frontend/src/components/eval/RunConfigModal.tsx`:
  - Trigger evaluation modal with target agent, suite selection, framework choice (`ragas`, `deepeval`, `both`), metric checkboxes, and threshold inputs
- [x] Update `EvalPanel.tsx` to compose all components with tab bar ("Dashboard & Trends", "Test Suites & Datasets", "Run History & Drill-Down")
- [x] Add API service methods: `getDashboardStats()`, `getDashboardTrends()`, `getDashboardComparison()`, `getRunMetrics()`, `triggerEvalRunEnhanced()`

## Dependencies
- S5-01f (Dashboard stats/trends API)
- S5-01d (Enhanced run with framework selection)
