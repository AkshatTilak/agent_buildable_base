# Subtask 03_01: Clean Startup & Workspaces Empty States

## Tasks
1. Verify Gateway initialization produces no default seed data (hubs, collections, agents).
2. Update `HubDirectory.tsx`, `IngestionOverview.tsx`, `AgentLibrary.tsx`, `WorkflowLibrary.tsx`, and `SuiteManager.tsx`.
3. Design and implement polished empty state components with clear call-to-actions (e.g. "Create your first Ingestion Hub", "Build an Agent") when no resources exist.
