# Subtask: Auth & User Profile UI (Login, Logout, Soft Delete UI)

- Clean up legacy auth code in `LoginPage.tsx`.
- Support rendering "Account Disabled" or "Account Deleted" states in the UI.
- Add a "Delete Account" button to the user profile view that triggers the soft deletion API.
