# Subtask: Gateway API Tests & Lifecycle Integration

- Write and expand specific tests in `tests/gateway/` to verify all gateway APIs directly (not just health checks).
- Tests must cover the full lifecycle (User Creation -> Hub Creation -> Agent Creation -> View -> Deletion) assuming a completely clean startup state.
