# Subtask: Infrastructure UI & Dependencies

- Update `InfrastructurePage.tsx` to handle redirects or expose commands to spin up non-core UI dependencies (like pgAdmin or Kafka UI if they exist).
- Ensure this page properly exposes the status of the infrastructure without relying on complex custom UIs that break easily.
